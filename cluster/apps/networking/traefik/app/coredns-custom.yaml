apiVersion: v1
kind: ConfigMap
metadata:
  name: coredns-custom
  namespace: kube-system
data:
    xxx.server: | 
         dh274.com auth.${SECRET_DEV_DOMAIN} {
              hosts {
                   10.2.111.29 ${SECRET_DEV_DOMAIN} auth.${SECRET_DEV_DOMAIN}
                   fallthrough
              }
          }

