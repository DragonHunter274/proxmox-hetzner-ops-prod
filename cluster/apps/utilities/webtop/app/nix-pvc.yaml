apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: webtop-nix
  labels:
    snapshot.home.arpa/enabled: "true"
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
