apiVersion: v1
kind: Secret
metadata:
  name: webtop-tailscale
stringData:
  # Replace this with a Tailscale auth key (tskey-auth-...) from the Tailscale admin console.
  # After updating it, encrypt with SOPS before committing.
  authkey: "tskey-auth-REPLACE_ME"
